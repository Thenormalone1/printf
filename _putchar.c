#include "main.h"

/**
 * _putchar - writes the characterto stdout
 * @c: char
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _putchar(char c)
{
return (c);
}
